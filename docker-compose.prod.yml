services:
  librarian:
    image: localhost:5000/librarian:0.1.0
    ports:
      - "8811:8811"
    env_file: .env.librarian
    volumes:
      - ~/data/librarian:/data/librarian
    restart: unless-stopped
